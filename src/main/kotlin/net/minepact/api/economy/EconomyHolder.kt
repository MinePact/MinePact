package net.minepact.api.economy

interface EconomyHolder {
    /*
    fun get(currency: Currency): Double
    fun set(currency: Currency, amount: Double)

    fun give(currency: Currency, amount: Double)
    fun take(currency: Currency, amount: Double)

    fun clear(currency: Currency)

     */
}